# Unknowns

- Exact generation timestamps for each trajectory.
- Exact provider model snapshots for the 14 files without structured model configuration.
- Whether the four `gpt-4o` replay configurations caused fresh inference or replayed stored responses.
- The exact operational meaning of positive `api_calls` in replay fixtures.
- Why demonstration/replay generation retained zero token and cost values.
- Per-request input, cached-input, reasoning, and output token counts.
- Current context occupancy, history re-send volume, tool-output token burden, and compactions.
- Retry, failure, and escalation events actually observed during the original runs.
- Complete wall-clock time; sparse per-step `execution_time` is not a run clock.
- Historical price table and whether any cost was billed during original generation.
- Objective verifier results, partial scores, and accepted/rejected outcomes.
- Whether companion artifacts outside the bounded directory resolve task success.
- Whether every raw email/path-shaped string is synthetic task content or public repository material; raw content was therefore not published.
- Whether underlying CTF/problem statements impose attribution conditions beyond the repository-level MIT license.
- Uniform schema behavior in earlier or later SWE-agent trajectory releases.
